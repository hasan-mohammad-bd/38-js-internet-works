//browser:
//static site: only make with html and css.

//browser is software: 
//the way Html keep its element is called DOM tree. 

//process how a page execute:
//dom tree and style structure combined make Render Tree(render: showing data) => paint(then paint all data). => display the data to the user 

//user interface: the displayed section to the user is called user interface.

//browser has engine: which execute the image,js file, html etc.

//if it is changed the any css html file. the render tree rerender and then execute the file.
